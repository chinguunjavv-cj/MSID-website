# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Local database and uploads
/data
/public/uploads

# Environment
.env.local
.env*.local

# Design source supplied by MSID â€” originals live outside the repo.
# Only the derived assets actually used by the site are committed, under public/brand.
/fwdhi
/unte_logo
/Logos

# Database dumps written by \
pm run backup\ — keep them off the laptop, not in the repo
backups/

# Per-machine Claude Code permissions
.claude/settings.local.json
